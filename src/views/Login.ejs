<!DOCTYPE html>
<html lang="es">
    <%- include('partials/head.ejs') %>
    
<body class="body-login">
    <%- include('partials/header.ejs') %>
    <form class="formulario" method="POST" action="/login">
    
    <h1 class="h1-login">Login</h1>
     <div class="contenedor">
     
     
         
         <div class="input-contenedor">
         <i class="fas fa-envelope icon"></i>
         <input class="input-register" type="text" placeholder="Correo Electronico" name="email" value="<%= locals.oldData ? oldData.email : null %>">
         </div>
         <% if (locals.errors && errors.email) { %>
            <div class="text-danger">
                <%= errors.email.msg %>
            </div>
            <% } %>
         
         <div class="input-contenedor">
        <i class="fas fa-key icon"></i>
         <input  class="input-register" type="password" placeholder="Contraseña" name="password">
         </div>
         <% if (locals.errors && errors.password) { %>
            <div class="text-danger">
                <%= errors.password.msg %>
            </div>
            <% } %>
         <input type="submit" value="Login" class="button">
         <p>Al registrarte, aceptas nuestras Condiciones de uso y Política de privacidad.</p>
         <p>¿No tienes una cuenta? <a class="link" href="/Register">Registrate</a></p>
         <p>¿Olvidaste tu contraseña? <a class="link" href="/passwordreset">Recuperala aqui </a></p>
     </div>
    </form>
    <%- include('partials/footer.ejs') %>

    <%- include('partials/lowerFooter.ejs') %>
</body>
</html>


